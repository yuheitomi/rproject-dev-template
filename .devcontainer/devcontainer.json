{
    "name": "${localWorkspaceFolderBasename}",
    "dockerComposeFile": ["./docker-compose.yml"],
    "service": "rstudio",
    "workspaceFolder": "/home/rstudio",

    // "none" or "stopCompose"
    "shutdownAction": "none",
    "remoteUser": "rstudio"
}
